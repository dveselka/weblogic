<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dave</groupId>
  <artifactId>dave-weblogic-graphql</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>war</packaging>
  <name>weblogicGraphQL</name>

  <parent>
    <groupId>com.oracle.weblogic.archetype</groupId>
    <artifactId>wls-common</artifactId>
    <version>14.1.2-0-0</version>
  </parent>

  <properties>
    <oracleMiddlewareHome>/app/weblogic-14.1.2</oracleMiddlewareHome>
    <oracleServerUrl>t3://127.0.0.1:7002</oracleServerUrl>
    <oracleUsername>weblogic</oracleUsername>
    <oraclePassword>weblogic123</oraclePassword>
    <oracleServerName>app-cluster</oracleServerName>
  </properties>

  <dependencies>
    <!-- Java EE 8 APIs - provided by WebLogic -->
    <dependency>
      <groupId>javax</groupId>
      <artifactId>javaee-web-api</artifactId>
      <version>8.0</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>com.oracle.weblogic</groupId>
      <artifactId>weblogic</artifactId>
      <version>14.1.2-0-0</version>
      <scope>system</scope>
      <systemPath>${oracleMiddlewareHome}/wlserver/server/lib/weblogic.jar</systemPath>
    </dependency>

    <!-- GraphQL Java - bundled in WAR -->
    <dependency>
      <groupId>com.graphql-java</groupId>
      <artifactId>graphql-java</artifactId>
      <version>21.5</version>
    </dependency>

    <!-- Jackson - bundled in WAR for JSON request/response handling -->
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>2.18.9</version>
    </dependency>
  </dependencies>

  <build>
    <finalName>weblogicGraphQL</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.11.0</version>
        <configuration>
          <source>21</source>
          <target>21</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.3.1</version>
        <configuration>
          <failOnMissingWebXml>false</failOnMissingWebXml>
        </configuration>
      </plugin>
      <plugin>
        <groupId>com.oracle.weblogic</groupId>
        <artifactId>weblogic-maven-plugin</artifactId>
        <version>14.1.2-0-0</version>
        <dependencies>
          <dependency>
            <groupId>com.oracle.weblogic</groupId>
            <artifactId>weblogic</artifactId>
            <version>14.1.2-0-0</version>
            <scope>system</scope>
            <systemPath>${oracleMiddlewareHome}/wlserver/server/lib/weblogic.jar</systemPath>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <phase>pre-integration-test</phase>
            <goals>
              <goal>deploy</goal>
            </goals>
            <configuration>
              <adminurl>${oracleServerUrl}</adminurl>
              <user>${oracleUsername}</user>
              <password>${oraclePassword}</password>
              <source>${project.build.directory}/${project.build.finalName}.${project.packaging}</source>
              <targets>${oracleServerName}</targets>
              <verbose>true</verbose>
              <name>${project.build.finalName}</name>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
